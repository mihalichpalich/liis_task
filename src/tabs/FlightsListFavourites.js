import React from 'react';
import {Text, View} from "react-native";

const FlightsListFavourites = () => (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Favourites</Text>
    </View>
);

export default FlightsListFavourites